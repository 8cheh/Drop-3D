# Frozen desktop build: exact dependency versions.
#
# These are pinned, not ranged, and that is a correctness requirement rather than
# tidiness.  The library's 282 tests were validated against numpy 2.2.6 and
# scipy 1.15.2; freezing the same measurement with numpy 2.5.3 / scipy 1.18.1 was
# measured to move gamma from 75.81 to 75.53 mN/m -- a 0.37% shift in the product
# this application exists to report.  A build that silently changes the answer is
# not reproducible, so the versions are fixed here and the freeze uses only this
# file.
#
# Refresh deliberately, and re-run the test suite plus a known-value check when
# you do.
numpy==2.2.6
scipy==1.15.2
pillow==10.4.0

# pywebview drives the Edge WebView2 runtime; pythonnet and clr_loader come with
# it on Windows and are what let a frozen executable still talk to .NET.
pywebview==6.2.1
pythonnet==3.1.0
clr_loader==0.3.1

# Build tool only -- never bundled into the output.
pyinstaller==6.22.3
