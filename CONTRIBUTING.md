# 协作规范 / Contributing

## 基本规则

1. **不直接推 `main`**。所有改动走分支 + PR，即使只有你一个人在做。
2. **每次动手都留日志**。在 `docs/log/YYYY-MM-DD.md` 追加。见 `docs/log/README.md`。
3. **影响接口或方法的决定写 ADR**。放 `docs/decisions/`，编号递增。
4. **调研先于编码**。物理上不确定的事，先开 research issue，结论写进 `docs/research/`。

## 分支命名

```
feat/<scope>-<简述>      新功能
fix/<scope>-<简述>       修复
docs/<简述>              文档
research/<主题>          调研
```

例如 `feat/tension-uncertainty`、`research/dynamic-validity`。

## 提交信息

```
<type>(<scope>): <简述>

<正文：为什么这么改，以及怎么验证的>
```

`type` 用 `feat` / `fix` / `docs` / `test` / `refactor` / `chore` / `research`。

## 数值代码的要求

本仓库的产品是**数字**，所以对数值代码有额外要求：

- **每个求解器/拟合器都要有一个「已知精确解」的测试**。例如 Young-Laplace
  在 `Bo = 0` 时精确退化为单位球——这是封闭解，比任何数值参考都有力。
- **改变数值结果必须给出改动前后的对比**。不允许「重构后结果略有变化」这种说法，
  要给出具体数字。
- **不确定度不是装饰**。报出的误差条必须经过覆盖性检验。
- **拒绝作答是合法结果**。当方法失效时，返回「无法测定」比返回一个数字好。

## 代码风格

- `ruff` 通过（配置见 `pyproject.toml`）。
- 类型注解尽量写全，特别是公开 API。
- **docstring 写「为什么」，不只是「是什么」**。算法出处、边界条件、
  以及踩过的坑都值得写进 docstring——那是唯一会跟代码一起被阅读的地方。

## 依赖

运行时依赖目前只有 `numpy` + `scipy`。新增依赖需要在 PR 中说明：

- 为什么现有依赖做不到；
- 许可证是什么（**必须打开它的 LICENSE 文件看，不要信包的元数据**——
  上游生态里有过 PyPI 标 MIT 而仓库实为 GPL-3.0 的先例）；
- 是否需要编译（会显著增加队友和 CI 的负担）。

## 数据

**不要把实验数据、大图、凭据提交进仓库。** `.gitignore` 已排除常见格式。
需要共享数据时用外部存储并在 `docs/` 里记录链接。
